/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

// module.exports = {
//   webpack: (config) => {
//     config.resolve.fallback = { self: false };
//     return config;
//   },
// };
